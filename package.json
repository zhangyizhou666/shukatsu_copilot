{
  "name": "jp-job-form-autofill",
  "version": "1.0.0",
  "description": "JP Job Form Autofill (Local) - Chrome extension for personal job form autofill.",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && cp -r public/* dist/",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "chrome-extension",
    "autofill",
    "jp-jobs"
  ],
  "author": "",
  "license": "MIT"
}
